<%= form_for @article, url:yield(:url) do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<br>

<%= f.label :content %>
<%= f.text_area :content %>

<%= f.submit yield(:button_text) %>

<% end %>
